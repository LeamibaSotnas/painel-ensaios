/**
 * @deprecated Este módulo pertencia à arquitetura anterior baseada em
 * Supabase. O projeto foi migrado para um banco local (SQLite) — veja
 * `src/core/db/client.ts` e `src/core/db/queries.ts`. Este arquivo é
 * mantido vazio apenas porque o ambiente de arquivos não permite excluí-lo;
 * ele não é importado por nenhuma outra parte do projeto.
 */
export {};
