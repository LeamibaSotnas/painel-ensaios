/**
 * @deprecated Substituído pela camada local SQLite (`src/core/db/*`) e por
 * `src/core/auth/get-usuario-atual.ts` para sessão/usuário atual.
 * Mantido vazio apenas por limitação de exclusão do ambiente de arquivos.
 */
export {};
